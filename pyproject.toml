[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name = "adcirc-subdomain"
version = "0.1.0"

[project.scripts]
adcirc-subdomain = "adcirc_subdomain.main:main"
